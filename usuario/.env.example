# =====================================================
# LUNARi User Service - Environment Configuration
# =====================================================
# Copy this file to .env and fill in your values
# =====================================================

# ==================== NeonDB PostgreSQL Configuration ====================

# NeonDB Connection URL
# Format: jdbc:postgresql://<host>:<port>/<database>?sslmode=require
# Example from NeonDB dashboard: jdbc:postgresql://ep-xxxx-xxxx.us-east-2.aws.neon.tech/lunari_users?sslmode=require
DB_URL=jdbc:postgresql://your-neon-host.neon.tech/lunari_users?sslmode=require

# Database username (from NeonDB dashboard)
DB_USERNAME=your-neon-username

# Database password (from NeonDB dashboard)
DB_PASSWORD=your-neon-password

# ==================== JWT Configuration ====================

# JWT Secret Key (minimum 32 characters for HS256)
# Generate a secure random key: openssl rand -base64 32
JWT_SECRET=your-secure-256-bit-secret-minimum-32-characters-change-in-production-please-update-this-value

# JWT Token Expiration (in milliseconds)
# 86400000 = 24 hours
# 3600000 = 1 hour
JWT_EXPIRATION=86400000

# ==================== Server Configuration ====================

# Server port (default: 8081)
SERVER_PORT=8081

# ==================== Notes ====================

# How to get NeonDB credentials:
# 1. Go to https://neon.tech and sign in
# 2. Select your project or create a new one
# 3. Go to "Dashboard" > "Connection Details"
# 4. Copy the connection string and credentials
# 5. Replace the values above with your actual credentials

# Security best practices:
# - NEVER commit .env file to version control
# - Use different credentials for development/production
# - Rotate JWT secret regularly in production
# - Use strong, randomly generated passwords

# Example NeonDB connection string breakdown:
# jdbc:postgresql://ep-cool-darkness-123456.us-east-2.aws.neon.tech/neondb?sslmode=require
# - Host: ep-cool-darkness-123456.us-east-2.aws.neon.tech
# - Database: neondb (you can rename this to lunari_users)
# - SSL Mode: require (mandatory for NeonDB)
