# ========================================
# Migration Script Configuration
# ========================================

# PostgreSQL Configuration (Source Database)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=lunari_users
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_postgres_password

# AWS DynamoDB Configuration (Target Database)
AWS_REGION=us-east-1
DYNAMODB_TABLE_NAME=lunari-users-dev

# Optional: LocalStack Configuration
# Uncomment these to use LocalStack instead of AWS DynamoDB
# DYNAMODB_ENDPOINT=http://localhost:4566
# AWS_ACCESS_KEY_ID=fakeAccessKey
# AWS_SECRET_ACCESS_KEY=fakeSecretKey

# Optional: AWS Credentials (if not using IAM roles)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# Migration Options
# DRY_RUN=true  # Set to true to test migration without writing to DynamoDB
